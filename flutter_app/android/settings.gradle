pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal() // Required for Flutter's Gradle plugin
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

include(":app")
